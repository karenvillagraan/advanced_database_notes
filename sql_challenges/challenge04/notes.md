Today I learned how ORDER BY helps to sort results in ascending or descending order, and how PARTITION BY is used inside analytic functions to divide data into groups without collapsing the rows like GROUP BY does, I also understood the idea of sliding windows, which allow calculations over a range of rows related to the current row, for example calculating running totals or moving averages, and overall I learned that analytic functions are very powerful because they let us analyze patterns in the data while still keeping every original row visible.
